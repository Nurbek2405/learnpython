'''x = 10  # Глобальная переменная

def print_global():
    print(f"Глобальная переменная x: {x}")

print_global()  # Вывод: Глобальная переменная x: 10
'''


'''def local_example():
    x = 5  # Локальная переменная
    print(f"Локальная переменная x: {x}")

local_example()  # Вывод: Локальная переменная x: 5
print(x)  # Ошибка: переменная x не существует вне функции
'''


# Создайте глобальную переменную total = 0.

# Напишите функцию add_to_total(value), которая прибавляет value к total и
# выводит результат. Используйте global.

# Напишите вторую функцию local_example(), которая создаёт локальную переменную total
# и выводит её значение, не изменяя глобальную переменную.

global_total = 0

def add_to_total(value):
    global global_total # Указываем, что изменяем глобальную переменную
    global_total += value
    return global_total

def local_example():
    total = 5 # Локальная переменная
    return total

# Вызов функций
print(f"Глобальный total после добавления: {add_to_total(10)}")  # Вывод: 10
print(f"Локальный total: {local_example()}")  # Вывод: 5
print(f"Глобальный total: {global_total}")  # Глобальная переменная не изменилась

# Ваш прогресс:
# Вы успешно освоили основы работы с глобальными и локальными переменными!
# Теперь ваш прогресс составляет 40%. 🎉

